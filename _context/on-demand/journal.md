# Journal: 日文學習 — history

> Purpose:回顧性紀錄 —「發生了什麼、為什麼」。歷史+理由的唯一的家。
> Update when:實質工作、決策或 commit 之後。只 APPEND(新的在下面)。絕不改舊條目。
> Focus:帶日期的條目+理由(+commit hash)。不自動載入 — 需要歷史/稽核時才讀。
> BUDGET:無 — 終端 sink,本來就會長。(單檔難讀時可按月輪替 journal-YYYY-MM.md,
>   並在 reference.md 記當前檔名。)

Entry format:
## YYYY-MM-DD HH:MM — <title>
- What / why / outcome (+ commit hash if any)

---

## 2026-08-21 17:58 — project 初始化
- What:以 project-scaffold skill 建置 v7 追蹤系統(CLAUDE.md、_context/、input/、output/)。
  reference.md 的初始內容由既有專案記憶(workflow.md / tts.md / website.md)萃取而來。
- Why:讓《大家的日本語》筆記維護與網站發佈的現況、待辦、決策有固定的家,新 session 冷啟動不必重讀全部記憶。
- Outcome:追蹤系統就位。2026-08-21 之前的歷史仍留在專案記憶三份檔案裡,本 journal 從今天起累積。

## 2026-08-21 — N5文法總整理.html 解凍改版(移除目錄、加發音、擴充動詞分類、手機收合)
- What:使用者主動要求修改根目錄的 `N5文法總整理.html`(原本在 reference F6 標記 FROZEN),四項變更:
  1. 移除「目錄」區塊(TOC 的 CSS/HTML/JS 全刪) — 使用者說跟分類 chip 功能重複、沒用。
  2. 加入發音:例句前 🔊 按鈕 + 點日文文字發音;變化表含假名的欄位也可點。
     右上角 🔊 開語音設定 modal(選日語語音/語速/音高/試聽),設定存 localStorage `n5_tts`。
  3. 「動詞分類」分類從 3 張卡擴充到 15 張:三大分類總覽、判斷三步驟、必背例外動詞、
     I/II/III 類常用動詞清單、V1〜V5 骨架、三類完整變化範例(書く/食べる/する・来る)、
     ます形時態、變化規則、禮貌體 vs 普通體、有時態 vs 無時態、日本學校文法對照。
  4. 手機版:標題列加收合鈕(▴),收起時隱藏分類 chip 列與進度文字;手機預設收合、
     選完 chip 自動收合;狀態存 localStorage `n5_header_collapsed`。
- Why:使用者這次在聊天中把動詞變化整套問過一輪(三類分法、ます/て/ない/た形、ば形/意向形、
  V1〜V5、時態四格、學校文法未然形/連用形對照),決定把這些對話內容沉澱回這份檔案。
  發音採 **Web Speech API**(瀏覽器 TTS)而非母版的 VOICEVOX 內嵌音檔 —— 因為這份是輕量單檔,
  不想像母版一樣膨脹到 17MB;作法參考母版的 `speakWith()` / 語音設定 modal,但簡化成單一語音。
- Outcome:檔案 58.9KB,JS 語法檢查通過(node --check)。未 commit(此檔不在 japanese-notes repo,
  是根目錄本機檔)。F6 的 FROZEN 狀態需更新 -> 見 reference.md。

## 2026-08-21 18:05 — 建立 N5總單字.html（767字）
- What:應使用者要求，在根目錄新建 N5 總單字表（單檔 HTML）。767 字依詞性分 8 組
  （名詞 476 含 14 主題小節、動詞 112 依グループⅠ/Ⅱ/Ⅲ、い形 54、な形 23、副詞 30、
  疑問詞 15、指示詞・接續詞 27、寒暄語 30）。每字含漢字/假名/東京式重音+♪音調圖
  （沿用母版 pitchNotes 做法）/繁中字義/例句+翻譯；功能：搜尋、詞性篩選、已熟打勾
  +進度條（localStorage）、遮中文/遮假名自測、點擊朗讀（speechSynthesis）。
- Why:與 N5文法總整理.html 成對，作課程進度外的 N5 全貌對照。JLPT 無官方單字表，
  內容依舊制四級+歷屆考題整理；重音為東京式，少數字辭典間有出入。
- Outcome:jsdom 全功能測試通過（渲染/搜尋/篩選/打勾/遮蔽）。無 commit（根目錄檔
  不在 japanese-notes repo 內）。註:本 session 早於 v7 初始化開始，reference.md F6
  已將此檔與 N5文法總整理.html 一併列為 FROZEN（暫停維護），後續以母版為準。

## 2026-08-21 (Cowork) — 母版 L3–L4 內容大擴充 + 動詞頁/時間頁 + 各小考 + tidy skill
- What:
  - 第3課:補課本+手寫單字(共97字)、文法6點(場所指示詞/どこ/どちら/どこの/いくら)、
    「家」うち＋いえ 同格雙讀音雙音調圖、101 發音修正(いちまるいち)、朗讀 rtmpl3。
  - 第4課(そちらは 何時までですか):全單字(~65字,含春夏秋冬/假期/節日)、
    動詞頁 VERB_DEFAULT[4](ます形四變化 標籤列式+6例句)、文法6點
    (休みます名詞vs動詞、から〜まで、名詞と名詞、大変副詞、ね/よ、何曜日/何時)、朗讀 rtmpl4、測驗。
  - 新增「動詞」分頁(單字↔文法之間):panel-verb + 動詞小考
    (該課ます動詞×四活用形;中文改帶時態翻譯 會〜/不會〜/〜了/沒有〜,不寫型態名)。
  - 時間頁:星期/時/分/時段(口語)參考表 + 隨機小考(星期/時段/時間可勾選、依勾選拼讀、
    段落間停一拍);數字頁小考(範圍輸入+拼讀發音)。4時/9時、20歲特殊唸法假名格換底色。
  - 建 skill `tidy-japanese-notes`(整理筆記 SOP);多次 UI 微調
    (手機播放器右側不被切、小考題號 n/總數+下一題+重考、各小考收合鈕、置中)。
  - 語音:診斷動詞小考活用形缺 clip → 寫 `語音包工具/add_verb_clips.py` + TODO;
    **已由雲端 session 補完(81 clips,驗證過)**。
- Why:使用者持續依課本進度餵單字/文法照片,要求邊做邊「整理筆記」(單字→文法→朗讀→測驗全同步);
  動詞變化與時間表達是 L4 重點,故另立專頁與小考;發音一律走母版既有 VOICEVOX 音檔優先機制。
- Outcome:母版(語音版)L1–L4 內容完整、各小考到位;JSON/JS/ends</html> 驗證通過;
  動詞小考音檔已補。無 commit(母版本機檔;網站同步待使用者決定)。

## 2026-08-21 21:32 — 廢除母版制度,改用 working 分支 + publish 流程
- What:
  1. 驗證線上版在本機 `file://` 下與母版功能完全相同(45 個可見按鈕、58 個拖曳把手、
     112 inputs 逐項比對,唯二差異是 `JP_READONLY=false` 與多一個 🏠首頁連結;
     訪客備註欄 `td.usernote` 在編輯模式為 0,疑慮解除)。
  2. 三個母版移進 `_to_delete/母版-0821/`:大家的日本語初級筆記.html(17.4MB)、
     N5文法總整理.html、N5總單字.html。後兩者與 repo 的 n5-grammar/n5-vocab 做過字元級比對,
     線上版是**純新增**的超集(只多首頁連結與手機 CSS),零刪除零修改。
  3. `語音包工具/` 搬進 repo 取得版控;移除已作廢的 build_voice_html.py、已完成的 TODO 檔、
     __pycache__;README 改寫(vv-data 數字改為實測 1655/4965/5)。
  4. 新增 `工具/git-wrap.sh`:`g`(自動清 lock)與 `publish`(commit-tree 壓成一筆推進 main)。
- Why:
  - 兩個 17MB 檔人工同步已經漂移過一次;轉檔配方 build_site.py 只活在雲端容器,
    每換 session 就要用 difflib 反推重建,是整條流程最脆的一環。線上版本身就能編輯,母版是純冗餘。
  - **過程中發現 `git checkout` 在這個環境會壞**:device_bash 沒有刪檔權限,切分支時 git 刪不掉
    舊分支才有的檔案,留成 untracked 之後 `merge --squash` 直接 Aborting。
    改用 `commit-tree` + `update-ref` 完成 squash,完全不動工作目錄也不換分支。這是這次最重要的發現。
- Outcome:
  - working 分支 3 筆 commit(9fb1d80 / 2d5de8a / 9058646),main 壓成一筆 86eeea1,
    `git diff main working` 為空,main 領先 origin/main 一筆,等使用者 push。
  - CLAUDE.md 新增「Git 流程」一節(三條鐵律);reference.md 全面改寫(F1〜F10、D1〜D10、T0〜T7)。

## 2026-08-21 23:54 — n5-vocab 總整理：補字 125、移除音調圖、全面改用冥鳴ひまり預錄音檔
- What:
  1. **補字**：抽 minna-notes `vocab-data` 第1〜12課（L5–L12 實際為空，等於 L1–L4 共 310 字）
     與 n5-vocab 的 767 字比對，補進 **125 字**（767 → **892**）。
     新增「國家・地名」小節（26 字，這份原本完全沒有國名）；「寒暄語」改名
     「寒暄語・常用句型」收 9 個句型。每筆都補了假名／重音／中文／日中例句，
     12 個專有名詞重音標「待確認」。
  2. **移除音調圖**：拿掉 `.pa2/.pm` CSS、表頭改回「調」、`build()` 不再呼叫 `pitchNotes`；
     函式與 `cho` 資料保留（比照 minna-notes 155d0c3）。
  3. **朗讀改預錄**：`speak()` 改為先查 `<script id="vv-data">`，查無才退回 Web Speech
     （原函式改名 `ttsSpeak`）。VOICEVOX 0.25.2 於雲端容器合成 **1725 段**
     （892 字的假名＋例句去重）speaker **14＝冥鳴ひまり**，opus 16k mono 內嵌。
     檔案 91KB → **6.8MB**。頁尾加「音声：VOICEVOX（冥鳴ひまり）」。
  4. 自測修正（使用者回報破版）：標籤「遮住假名＋音調」→「遮住假名」；
     「調」欄退出遮罩（`panote kcell` → `panote`）。
- Why:
  - 補字來源選 minna-notes 而非 JLPT 詞表，是使用者的決定：只補「課本已經學過、這份卻沒有」的字。
  - 品牌字 `アップル`／`ルイスウトン` 依使用者指示不收（分類原則已記進 todo）。
    專有名詞 101／グランドホテル／スーパーマン／海賊 保留，理由是課本已學過。
  - 音調圖與 minna-notes 同步移除（改練聽力）；移除後「調」欄只剩一個數字，
    再套遮罩既無自測價值又會讓「點一下顯示」溢出格子，故退出遮罩。
  - **`vvNorm` 這份多剝一個 `〜`(U+301C)**：n5-vocab 用的是 U+301C，minna 用 U+FF5E。
    合成端與前端用同一套規則，否則就是「音檔存在但查不到」的靜默失敗（T7）。
- Outcome:
  - Playwright 驗證 21 項全過：892 列、音調圖殘留 0、**2676 個 `data-say` 音檔覆蓋率 100%**、
    抽驗 clip 為合法 Ogg 且 Chromium 可解碼（0.67/1.92/3.38 秒，單聲道）、
    搜尋・分類・遮罩・打勾進度正常、320/360/390/414/1280 無橫向溢出、console error 0。
  - commit `320e293`（l4-cont-0821）→ `publish` 壓成 main `dfffa6b`；
    main 領先 origin/main 一筆，**等使用者 push**。
  - 合成耗時約 55 分鐘（1725 clip × 約 1.9 秒），看門狗未觸發、引擎全程未掛。

## 2026-08-22 22:00 — 搬到本機 Claude Code:音檔管線接通、Emily 試作後放棄、_to_delete 清空

- What:
  1. **本機工具鏈裝好**:VOICEVOX ENGINE 0.25.2 CPU(winget `HiroshibaKazuyuki.VOICEVOX.CPU`,
     解壓縮型套件,engine 在 `WinGet\Packages\...\VOICEVOXv-engineun.exe`)、
     ffmpeg(Gyan 9.0,含 libopus)、node v24.19.0、playwright(裝在 `語音包工具
ode_modules`)。
  2. 新增 `本機補音檔.py`:一行跑完「找 ffmpeg → 自動起引擎(無介面,4 秒,常駐)→ export → merge」。
     `export_missing_clips.py` 的 `/tmp` 改 `tempfile.gettempdir()`;
     `test_voice_full.mjs` 改本機版(自帶 chromium、HTML 吃參數、拿掉寫死的 clip 數斷言)。
     commit `0331388`、文件 commit `73a89de`。
  3. **試加第四個聲音 Emily(冥鳴ひまり 14)→ 使用者喊停,已完整還原。**
     產了 621 個 clip(25 分鐘)後才發現覆蓋率只有 621/1686;補寫 `fill_voice_clips.py` 要補剩下
     1065 個時,使用者因為太慢而放棄。`git checkout` 還原 HTML 與兩支腳本,驗證回到 5058 clips/三角色。
  4. **`_to_delete\` 清掉 ~74MB**:git 殘留 ×5、_publish_test.tmp、舊 skill、語音包工具-0821。
     只剩三份舊母版(16.8MB)等使用者決定。
  5. main 快轉合併 working(5 筆)並 **push**:`dfffa6b..73a89de`。第4課文法內容一併上線。

- Why:
  - 搬到本機是因為 Cowork 產音檔綁在雲端容器(沙盒跑不動引擎、20MB 過橋上限),
     本機引擎常駐後這些限制全消失。
  - Emily 放棄的關鍵數字:**每 clip 2.5〜3 秒**(引擎單一請求就吃約 3.8 顆核心、自己排隊,
     workers 1/4/8 沒差,拉高優先權、重啟引擎都沒用),一個聲音 50〜80 分鐘、檔案 +5.8MB。
     我先前用一次基準測出的 0.4 秒/clip 估成 14 分鐘,是低估,已更正並記進文件。
     (對照:2026-08-21 雲端 1725 clip 花 55 分鐘 ≈ 1.9 秒/clip,本機並沒有比較快。)
  - `export_missing_clips.py` 的來源是「單字＋data-say」只有 621 個文本,vv-data 卻有 1686 個
     (五十音、朗讀逐行、時間/數字拼讀單位來自 `gen_full_manifest.py` 那條完整管線)。
     這個落差是加聲音的真正成本所在,已寫進 `語音包工具\README.md` 與 `toolchain.md`,
     `fill_voice_clips.py` 本身則刪掉(死碼)。
  - 刪檔規則改寫:「我沒有刪檔權限」是 Cowork 沙盒限制,本機不成立。CLAUDE.md 已更新為
     純垃圾直接刪、有內容價值的先確認。
  - commit 前等使用者確認、push 這次由使用者明講要我做,故直接執行(以往是他自己按)。

- Outcome:
  - `python japanese-notes\語音包工具\本機補音檔.py` 是之後補音檔的唯一入口;
     驗證 `node …	est_voice_full.mjs`,24 項全 PASS(clips 5058 / texts 1686)。
  - origin/main = `73a89de`,工作目錄停在 `main`,乾淨。
  - 端對端測過:挖掉「しゅくだい」3 個 clip 的副本能被管線補回並再次全 PASS。

## 2026-08-22 22:15 — `_to_delete\` 全部清空
- What:使用者確認後刪掉最後三份舊母版(`大家的日本語初級筆記.html` 16.63MB、
  `N5總單字.html`、`N5文法總整理.html`),資料夾本身保留。加上稍早刪的 git 殘留等,
  今天總共清掉約 **91MB**。
- Why:母版制度 2026-08-21 已廢除(D1),三份檔案的內容都由 repo 現役檔取代;
  它們沒進過 git,但留著只會讓人以為還有第二份真相。
- Outcome:reference.md 的 F8/F9/F10/T0/D3 已改寫成刪除後與「本機可刪檔」的事實,
  新增 F11(本機補音檔.py 是補音檔唯一入口)。

## 2026-08-22 23:10 — 五十音獨立成頁、動詞總表、git 改為 main 直做

- What:
  1. **`kana.html`(2.02MB)**:第0課從 minna-notes 抽出來獨立成頁。104 個假名(清音46/濁音20/
     半濁音5/拗音33)＋例詞表、四種題型測驗(讀音/聽力/平↔片/打字)、範圍可選,自帶 1179 clips。
     新增 `test_kana.mjs`(19 項)。首頁第一張卡改成它。
  2. **minna-notes 移除第0課**:資料 104 列、renderGojuon/buildKanaQuiz/kanaScope 等程式、
     panel-gojuon HTML、13 行 CSS,以及 335 個只有五十音在用的文本(1005 clip)。
     後續又清掉 allWords() 裡併五十音例詞的區塊與 GOJUON_META(3.4KB)。**17.91 → 16.26MB**。
  3. **動詞總表**(總學習 → 文法總表右邊):第1〜12課所有動詞(11 個)× 四種ます形,點格發音;
     手機版改卡片式(flex order 重排)。附小抽考:44 題跨課洗牌、三模式、掀答案顯示
     漢字＋假名＋中文(第n課)。移除空的「其他單字」分頁。
  4. commit `e252b29`、`2048af4`,**已 push**(origin/main = 2048af4)。
  5. git 流程改成**直接在 main 上做、不開分支**(使用者定案),CLAUDE.md 與 D10 已改寫。

- Why:
  - 拆五十音的理由不只是檔案大小:①使用情境不同(頭幾週天天開,不該為它載 17.9MB)
    ②第0課是資料模型裡唯一的異類 schema(hira/roma/kata),到處要特判 ③首頁動線。
  - 裁音檔沒有用猜的:先 instrument `vvClip` 跑完整個頁面(每課每分頁、400 次時間小考、
    600 個隨機數字、所有朗讀套組、70 次出題),只從 393 個五十音文本裡刪沒被查詢過的。
    像 に/ご/ろく/ひゃく/じ 這些同時是數字時間讀音的 58 個因此自動保留 —— 直接照
    「文本屬於第0課」刪會靜默弄壞數字小考。
  - 動詞的判定沿用既有規則(假名以「ます」結尾、長度 ≤9),沒有另外加 pos 欄位,
    因為現有 pos 只有 x/n/job/country/place,沒有動詞分類。
  - 手機版六欄會擠到每個字換行,所以 600px 以下轉成卡片;不是用 overflow-x(T5)。

- Outcome:
  - test_voice_full 25 項、test_kana 19 項全 PASS;兩份頁面補音檔皆 0 缺。
  - 320px 仍有 39px 橫向溢出,來源是課程分頁那條 tab 列(切到任何分頁都一樣),與本次改動無關。

## 2026-08-22 23:15 — 更正:先前評估的是「測驗」分頁,不是單字裡的小抽考
- 使用者問「單字裡面的小考」時,我評估的是「測驗」分頁那個 10 題選擇題(buildQuiz/makeN5Q),
  但他問的是單字分頁裡的 `#voc-quiz` 小抽考(zh2jp/jp2zh/listen 三模式)。
- 因此「完全沒有意思題型」對小抽考不成立;小抽考的實際弱點是純自評、無對錯記錄、
  切課重新洗牌、無間隔重複。測驗分頁那五點仍然成立。todo 已改成兩個元件分開記。

## 2026-08-22 23:55 — 動詞總表小抽考、聽寫打字模式、切課自動發音修正

- What:
  1. 動詞總表加小抽考:題庫 44 題(11 動詞 × 4 變化)跨課洗牌,結構與各課動詞分頁那個相同
     (ids av-*),掀答案顯示漢字＋假名＋中文(第n課)。後來依使用者要求拿掉上方說明列與計數。
  2. **四個小抽考都新增「聽寫打字」模式**(單字 vq / 單字總表 aq / 各課動詞 vb / 動詞總表 av):
     聽發音 → 打字 → Enter 對答案 → Enter 下一題。**假名或漢字都收**,比對前只去掉空白與
     「～・」,其餘嚴格比對。答錯顯示正解。輸入框不放 placeholder(使用者要求留白)。
  3. **Bug 修正:切課會突然播放音檔。** 只聽發音與聽寫打字都是「重繪就發音」,而切課會觸發
     render → *Pick → 重繪。改成閘門 `aqAutoSpeak`,只有按「下一題／重考／切模式」才開。
     新增 `test_autoplay.mjs`(12 項)防退化。
  4. commit `2048af4`(動詞總表)、`7626738`(聽寫打字＋修 bug),**都已 push**。

- Why:
  - 打字判分收假名也收漢字,是使用者選的:他兩種都會打,且不想引進羅馬字轉換
    (原本評估要做 kana→romaji 表加寬容比對,約 110 條音節 + 促音長音規則,決定不做)。
  - 嚴格比對促音/長音/片假名是刻意的 —— 聽寫的價值就在 きって／きて、おばさん／おばあさん
    這種只有寫下來才會露餡的地方。
  - 總表頁的聽寫等於開卷考(答案表格就在下面),使用者說不用管。
  - 自動發音那個 bug 其實舊有(只聽發音模式本來就會),聽寫模式只是讓它更容易撞到;
    修法選閘門而不是拆掉自動發音,是為了保留「按下一題就直接聽到」的手感。

- Outcome:
  - test_autoplay 12 項、test_voice_full 25 項、test_kana 19 項全 PASS;四個 JS 區塊 node --check 通過。
  - origin/main = `7626738`。

## 2026-08-23 17:05 — 文法小考(各課＋總表)、第4課朗讀重產、測驗補題與洗牌、移除 VOICEVOX 署名

- What:
  1. **文法小考**:新增 `gen_grammar_quiz.py`,把 29 個文法點展開成 **122 題**(第1課23/第2課29/
     第3課26/第4課44),寫進 `gquiz-data`。兩個地方吃同一份題庫:各課「文法」分頁(只出該課)、
     總學習→文法總表(122 題跨課,標示來源)。兩種模式:看中文→說日文、只聽發音(掀開顯示日文＋中文)。
  2. **第4課詞性標細**:72 個字從幾乎全是 x 拆成 動詞9/加に的時間11/不加に的時間17/名詞15/
     場所5/疑問詞4/接尾6/副詞1。第1課補 person(わたし/あなた/あの人)。
  3. **第4課朗讀重產**:樣板 8→12 個,獨立成 `gen_reading.py`(固定種子),HTML 的 JS 樣板同步。
  4. **測驗**:第4課 12→18 題、第2課 0→9 題;總學習測驗 20→30 題並改成 10 文法＋20 單字;
     文法題出題時洗牌選項。
  5. 移除兩頁的頁尾 VOICEVOX 署名(使用者要求);順手清掉 n5-vocab.html 檔尾 293 個 NUL。
  6. commit `ea814af`、`6c6d2a3`,都已 push。

- Why:
  - 題目**在 build 時展開**而不是前端即時組合:①每句都能先印出來人工檢查 ②前端即時組的句子
    不會出現在 HTML 裡,`export_missing_clips.py` 掃不到就會缺音檔(時間/數字小考當年就是踩這個)。
  - 產生器內建**動詞分類**(瞬間動作配「○時に」、持續動作配「から/まで」),否則會生出
    「毎日 6時に 飲みます」「我7點結束了」這種句子;課本第4課⑦本身就有這個對照。
    另外限制主語(「我是老師嗎?」很怪)、時間順序(「從9點到6點」)、中文量詞(「這個書」)。
  - **文法題的正解原本全部寫在第一個選項**(27 題全是 ans:0),等於背位置就能全對 → 出題時洗牌。
  - `export_missing_clips.py` 現在也掃 vv-data.reading:朗讀音檔以前是另一條管線產的,
    這支掃不到,所以改朗讀會靜默缺音檔。
  - 移除署名與 VOICEVOX 利用規約的標註要求相衝突,已告知使用者,由他決定。

- Outcome:
  - 音檔 +396 clips(文法小考 303、朗讀 93),共 4449 clips / 1483 texts,minna-notes 18.15MB。
  - test_voice_full 32 項、test_kana 19 項、test_autoplay 12 項全 PASS。
  - origin/main = `6c6d2a3`。
  - 過程中插第2課題庫時正規表示式非貪婪匹配停錯位置,把題目插進第1課中間(第1課一度剩1題),
    已修正並確認四課題數 8/9/7/18。

## 2026-08-23 18:20 — 文法小考擴到 204 題、朗讀總表重產、「課程更新要連帶更新總學習」入規

- What:
  1. 使用者對照課本指出第4課小考的缺口,補齊後 122 → **204 題**(各課 33/42/37/92):
     「〜まで」配動詞、動詞版的から〜まで(含課本原句「昨日 日本語を 何時から 何時まで 勉強しましたか」)、
     ⑧ 補 クラス/銀行與時段版(4→30 題)、⑥ 的問句變化(2→10 題)。每個樣板產出 4→6 句。
  2. **朗讀總表(第99套)重產**:稽核發現它還留著「6時から 4時までです」(時間倒著走),
     且各課涵蓋失衡。改成 `gen_reading.py` 的 `build_total`:**把各課已產好的朗讀輪流混合**
     (以「A: 問/B: 答」為單位)。2 套 × 90 行,四課都有,缺音檔 0。
  3. `CLAUDE.md` 新增一節「課程更新的連帶工作」:任何一課的內容一動,總學習要一起更新;
     並列出哪些會自動跟上、哪些一定要重跑產生器。
  4. commit `0fec53e`,已 push。

- Why:
  - 第99套原本是另一套樣板獨立產的,所以會跟各課朗讀各自演化(時間倒序的 bug 修了各課卻沒修它)。
    改成混合各課現成內容之後,**每一行必然有音檔、每一課必然被涵蓋**,這個類別的漏更新從結構上消失。
  - CAP 4→6 後才冒出「休みは 金曜日と 夏です」(星期與季節同槽)、「わたしは 終わります」、
    「何時から 何時まで 飲みますか」—— 放大取樣會把樣板的鬆散處放大,所以擴充後一定要重看輸出。
  - 使用者要求把「課程更新要連帶更新總學習」寫進 CLAUDE.md,因為真正會漏的不是總表本身
    (那些是即時算的),而是三支產生器 —— 尤其文法小考題庫,不重跑新單字永遠不會進題目。

- Outcome:
  - 稽核六個區塊全部一致:單字總表漏 0、文法總表 4 課、動詞總表逐課一致、總測驗 30 題(10 文法涵蓋 4 課)、
    文法總表小考 204 = 各課小考總和、朗讀總表四課皆有。
  - clips 4686 / texts 1562;minna-notes **20.36MB**(音檔外置的優先度因此提高)。
  - test_voice_full 32 / test_kana 19 / test_autoplay 12 全 PASS。origin/main = `0fec53e`。

## 2026-08-30 16:30 — 第5課上線(單字→文法→小考→朗讀→測驗全套)、文法小考跨課取材

- What:
  1. **第5課「この 電車は 甲子園へ 行きますか」**:單字 72 筆(課本 45＋会話 7＋練習C 1＋手寫筆記 19)、
     文法 8 點、文法小考 93 題、朗讀 3 套×23 行、測驗題庫 14 題。詞性沿用第4課的細分。
  2. **文法小考跨課取材**(使用者要求順便複習):產生器加 `SCOPE`,第5課的槽可用第3・4課的
     場所與時間。題庫 204 → **298 題**。
  3. 依使用者指示修剪第5課:刪掉前面課程已出現的 6 個字、刪掉関連単語(甲子園/大阪城)、
     「普通」補「普通車、普通」、「どう いたしまして」註明字面意思與一般會說「いいえ」。
  4. commit `b230648`(單字)、`7c7c009`(文法等),都已 push。

- Why:
  - 重音(cho)全部標「待確認」:照片上有重音線,但逐字反推「第幾拍下降」容易數錯,
    tidy skill 自己就寫「錯的重音比沒有更糟」。要補的話得另做一輪放大判讀。
  - 跨課取材後冒出三類怪句,各自加了限制:交通工具分「搭/騎」讓中文正確;
    目的地排除教室・トイレ・エレベーター(否則「搭公車去廁所」);長途只配飛機。
  - 「指定假名清單」的槽本來是先照詞性撈再過濾,交通工具跨了 n/place 好幾種詞性所以撈不到,
     改成不分詞性去全表找。

- 踩到／查證到的:
  - **`export_missing_clips.py` 以前自己硬編碼一份 sayForceKana**,與 HTML 裡的 JS 那份漂移。
    結果新加的日期字會拿漢字去合成 ——「1日」唸成「いちにち」而不是「ついたち」。
    已改成直接讀 HTML 那份(缺的 clip 從 336 降到 258)。
  - **HTML 裡的 `rtmpl1..5` / `buildReading1..4` 是死碼**,從來沒被呼叫;`genReadingText()` 只從
    vv-data.reading 取預錄套組,連「🔄 從該課單字產生」按鈕也是換一套預錄的。
    朗讀的唯一真實來源是 `gen_reading.py`。(先前為了「同步」還特地改過 JS 那份,其實沒有作用。)
  - test_voice_full 有一條斷言假設第5課沒有文法小考,內容補上後就 FAIL;
    已改成用第6課驗空狀態、檢查範圍擴到 1〜5 課。

- Outcome:
  - 音檔 +825 clips(第5課單字 258＋文法/小考/朗讀 567),共 **5511 clips / 1837 texts**,
    檔案 **24.24MB**(音檔外置的優先度再往上)。
  - 總學習連帶更新:動詞總表 14→20、單字總表 370→386、文法總表小考 204→298、朗讀總表涵蓋五課。
  - 三個測試 32 / 19 / 12 全 PASS;origin/main = `7c7c009`。

## 2026-09-01 23:10 — 第5課調校:時態 bug、文法重排、助詞分頁;以及「點了沒發音」的漏綁

- What:
  1. **時態 bug(使用者在小考裡抓到)**:「昨日 実家へ 帰ります」—— 時間詞與動詞時態沒配對。
     兩支產生器都把時間詞拆成未來組(明日/週末/あとで/今日)與過去組(昨日/先週/おととい),
     各配正確動詞形並補過去式樣板。寫檢查掃過全部題目與五課朗讀:時態不合 0 句。
  2. **第5課文法重排**(使用者指定 ⑥①②⑦⑧③④⑤ → ①〜⑧),新①(人數・範圍＋で)補上
     「沒有で」的對照四組共 8 例句;測驗題庫 14→20 題、小考 93→110 題、總題庫 315 題。
  3. 單字修剪:刪「塾」(第3課已有)、加「二人(ふたり)」、清掉 そうですね 的備註。
  4. **新增「助詞」分頁**(第5課):9 個助詞 15 列,同助詞用 rowspan 合併,
     本課教的三列(で工具/に著陸點/を手掌)用時間表同款底色 #fff3e0 標示;27 句例句都有音檔。
  5. commit `2186b69`、`c0c4a5d`,都已 push。

- Why / 踩到的:
  - **新面板漏綁發音事件**:發音是「各面板各自綁 .gspk 的 click」,新增的助詞面板漏了,
    結果 data-say 與音檔都在、點下去卻沒反應。**我的驗證只查 vvClip 查得到音檔,抓不到漏綁** ——
    改成用 Playwright 真的點按鈕、計數 speak() 被呼叫幾次。以後新面板一律這樣驗。
  - 助詞表格式改過三版(逐列 → 加 Logic 核心六欄 → 同助詞合併)。使用者的標準是
    「一眼看得懂、例句不要換行」,所以欄寬要手動配:助詞 57px、例句 395px。
  - 底色的語意是「**這一課教的**」,不是「已學過的」——「に 時間標靶」是第4課學的所以不標。

- Outcome:
  - clips 5876 / texts 1959,minna-notes **26.16MB**(音檔外置越來越該做)。
  - 三個測試 32 / 19 / 12 全 PASS;origin/main = `c0c4a5d`。

## 2026-09-04〜09-06 — 第5課補字與月份、時間頁月／日表、文法⑨〜⑫、朗讀150／測驗50、手機版小考版面
- `40321dc`(09-04):第5課單字補「同事」與 1〜12 月;「數字→時間」頁最前面加 1〜12 月、1〜31 日兩張表
  (特殊唸法用 `tok-hl` 上色,同 よじ),小考多「月」「日」兩個選項;`export_missing_clips.py` 補掃執行時才產生的
  月日唸法(表格是 renderTime() 用 innerHTML 產的,靜態掃 data-say 掃不到 —— 跟朗讀當年同一個坑)。
- `4022320`(09-04):第5課文法 ④ 改寫成「時間加不加に」三分類;新增 ⑨［人］と、⑩ 誰と、⑪ 何月何日／いつ、⑫ 語順
  (第5課共 12 點);測驗補 11 題(第5課 31 題);朗讀總表 `gen_reading.py 99` 改 2 套×150 行;
  總學習測驗 30→50 題(15 文法＋35 單字)。文法小考題庫 355 題(33/42/37/93/150)。
- `dbb1d74`(09-04)＋`38aab75`(09-06):使用者用 iPhone 回報小考每題高度不一 → 600px 以下 `.aq-body` 改
  display:block、答案框滿版、文法點標籤獨立一行。第二次修:`#id .aq-body` 的權重蓋過 `.aq-min .aq-body{display:none}`,
  收合後題目還在 → 六個選擇器加 `:not(.aq-min)`。教訓:加 id 選擇器前先找有沒有會被它蓋掉的狀態規則。
- 問答(沒改碼):手機版預覽按鈕 → 不做(DevTools Ctrl+Shift+M 就夠;27.8MB 的頁用 iframe 再載一次不划算);
  軟體公司分不分手機/桌機版 → 主流是單一響應式,只在使用情境真的不同時換元件,獨立 m. 站已淘汰。

## 2026-09-08 — 整個專案資料夾合併成 git repo(在 Windows 上做),網站改從 /docs 發布
- Why:要讓 Windows + 新 MacBook 兩台靠 git 同步全部內容(含 CLAUDE.md、_context/);Mac 那邊也要離開 iCloud(iCloud 會弄壞 .git)。
- 調查結論:這台 Windows 的資料夾不在 iCloud、是最新的活資料;Pages 原本是 main / root;repo Public;
  output/ 兩張 PNG 含第三者名字與出國日期 → 決定 output/ input/ _to_delete/ 一律 .gitignore。
- 做法(兩段式,網站零斷線):
  1. `128dd8b` 先把 26 個追蹤檔**複製**到 docs/(同 blob 不佔空間)+ docs/.nojekyll,root 不動 → push
     → 使用者到 GitHub Settings 把 Pages 切成 main /docs(檢查法:`/docs/index.html` 從 200 變 404 就是切好了)。
  2. `.git` 搬到專案根、刪掉 root 那份、加 root .gitignore / .gitattributes、CLAUDE.md 與 _context 進 git;
     路徑 `japanese-notes\` → `docs\`(CLAUDE.md、progress、reference、toolchain、README、git-wrap 等);
     test_autoplay.mjs 改成 HERE 相對路徑;_context 裡被吃掉的 `\t \n \v \r` 修回來 → Pages 切好後再 push。
- 順手:`git config --global core.precomposeunicode true`;三套測試從 docs/語音包工具 跑全過(37/19/12)。
- 沒做:`git gc`(321MB loose objects,使用者決定)、`working` 分支(已是 main 的祖先,可 `-d`)。
- Skill `tidy-japanese-notes` 的 SKILL.md(在 %APPDATA%\Claude 的 skills 快取,不在 repo)寫死 Windows 路徑與母版舊制,已改;
  若之後被帳號同步蓋回去,要到 app 的 Skills 介面改。
- 踩坑:Bash 工具的 heredoc 會把 `\\` 縮成 `\`,含反斜線的 Python 一律用 Write 寫成檔案再跑(見 toolchain)。
- 收尾:第二步 commit `365998d`;Pages 切到 /docs 後線上 6 頁全 200、首頁 md5 與本機一致、`/CLAUDE.md` 404;
  `git log --follow docs/minna-notes.html` 一路 34 筆到第一個 commit。同日 session-wrap。

## 2026-09-10 — MacBook 環境建好(在 Mac 上做),Mac 也能產音檔;順手修 kana.html 補音檔的兩個舊 bug
- Why:使用者要在咖啡廳用 Mac 接著做。結論:Claude Code 的本機 session 綁機器與絕對路徑,Windows 關機後 Mac 接不到那個 session;
  改成兩台各開各的 session、靠 git 同步(雲端 session 也可但每次看檔要 push/pull 一輪,不適合改 HTML 為主的工作)。
- 環境:Homebrew 6 → node 26.8 / ffmpeg 9.0.1(libopus)/ 7zip;`npm ci` + Playwright Chromium;repo 內 user.name/email 設同 Windows;
  `core.precomposeunicode=true`。`4a75ad2` 先記到 toolchain §6。
- VOICEVOX:release 的 macos-arm64 0.25.2 7z(1.8GB)解到 `~/Applications/voicevox_engine/macos-arm64/`,`xattr -dr com.apple.quarantine`;
  `本機補音檔.py` 的 `ENGINE_GLOBS` 非 Windows 時改找那裡。四個 speaker id 都在,「水餃→ミズ」與 Windows 一致。
- 踩到兩個舊 bug(Windows 上對 kana.html 跑也會中):
  1. `export_missing_clips.py` 第 D 段(月份/日期 45 個文本)無條件加 → kana.html 多合成 135 個 clip、胖 336KB。改成頁面有 `monthKana(` 才加。
  2. `merge_clips.py` 寫後驗證寫死找 `vocab-data`,kana.html 是 `kana-data` → 寫完檔才 exit 1。改成依頁面有哪個區塊各自驗。
  kana.html 已 `git checkout` 還原;修完三頁重跑皆缺 0、HTML 沒動;merge 修正用副本+假 clip 實測。
- 驗證:test_voice_full 37/0、test_kana 19/0(Mac)。
- 未裝:pillow-heif(要看 HEIC 手寫照才需要);Python 用系統 3.9(Windows 3.11),目前所有腳本可跑。

## 2026-09-11 — N5 模擬考題庫化:150 題、配額抽題、上次考卷回看;Mac 首次獨立產音檔
- Why:使用者報名 2026-12-06 N5,原 `n5-mock-easy.html` 只有固定 30 題、每次同一份。決定做題庫＋每次抽題(不做固定套卷,加題不用平衡整卷),
  範圍全 N5(1〜25 課),規模 150 題 = 5 份的量。
- 題庫:原 30 題保留、新增 120 題,配額與原一份相同(文字語彙 4/2/1/1、文法讀解 6/1/3、聴解 3/3/3/3);讀解以「一篇 3 題」為單位抽。
  抽題邏輯 `QUOTA`/`unitsOf`/`pickExam`;交卷 `markSeen` 記進 localStorage `jp_n5mock_seen`,優先抽沒考過的,全考完自動重來。
- 上次考卷回看(使用者加的需求):交卷時整份存 `jp_n5mock_last`(題目、作答、日期、是否自動交卷),開始畫面「上次考試結果」按鈕;
  批改與成績卡從 submit 抽成 `showGraded(rec, review)` 共用,回看不動最佳紀錄、不重記考過。只留最近一次。
- 音檔:新增 `mock_audio.py`(角色 F/M/N = speaker 2/11/13,從既有 clip id 用 md5 規則反推),補 157 clips,檔案 360KB → 1.75MB。
  兩個坑:①node 讀 stdin 沒 setEncoding,日文字切在分塊邊界變亂碼、合成出壞 clip(已清、已修);②「何まい」唸成ナニマイ → 改假名。
- 我自己的 bug:題目識別 key 一開始只用題目文字,發話表現/即時應答的 q 全相同會撞號 → key 改為 短文＋題目＋對白。
- 驗證:新增 `test_mock.mjs` 23 項全 PASS(結構、配額、音檔覆蓋、抽 30 題、交卷記錄、兩次不重複、回看、版面)。
- 沒動:考試說明裡「瀏覽器內建語音」舊文案;CLAUDE.md 驗證清單未加 test_mock(記在 toolchain)。toolchain.md 201 行,貼在 budget 邊。

## 2026-09-11（第二筆）— 模擬考改成正式 N5 的縮小版:14 個大題、三科依序、180 分制、聴解圖片題
- Why:使用者要「除了題數以外完全和正式考試一樣」。定案:每大題取正式的一半(中文讀解維持一篇兩題,共 38 題)、
  時間減半 10/20/15 分、三科依序不能回頭、計分 120＋60、合格線 80/38/19(比例換算,註明估計)、音檔可重播(使用者要自己控制)。
- 題庫 150 → 195:去掉非正式的「中翻日選正確句」5 題、中文讀解每篇砍成 2 題並改成 問題5;新增 文字語彙 20、句子重組(★)10、
  文章文法 5 篇×2、短文 5、情報検索 5(HTML 表格)、課題理解 5。BANK 依 科目→問題N 排序,`QUOTA`/`LABEL` 各 14 個 key。
- 流程:`cur`/`LIMITS`/`nextSection()`/`lockTabs()`;`showGraded` 算 `know`/`listen`/`passed`,成績卡列 14 個大題答對數;
  BEST_KEY 改 `jp_n5mock_best2`(180 分制)。
- 圖片:我不能產圖,使用者用 ChatGPT 依我寫的 prompt 產 29 張(1 張單圖＋6 張 2×2 合圖,標籤有錯但內容順序對)。
  用 Pillow 偵測格線切成每題一張(上兩下一的排法要上下列分開找直線;場景裡的門框會被當成格線 → 只認貼邊與中間 35〜65% 的線)。
  題庫欄位 `img`+`fixed`(四格題,c=1〜4)、`scene:{img}`(發話表現)。
- 聴解:`audioLines()` 統一播放順序(問題1・2 旁白前後唸題目;readChoices 唸選項),`mock_audio.py` 同步,補 32 個旁白 clip → 246;
  問題3・4 選項作答中只顯示號碼(`.choices.row.hid`),交卷後才出字。
- 驗證:`test_mock.mjs` 35 項全 PASS(計時器檢查改成允許 1〜2 秒誤差,原本會偶發 FAIL)。截圖確認四格圖、場景圖、交卷後原文。
- 沒動:「看原文」作答中仍可按(會露出選項文字);toolchain.md 203 行超 budget 3 行,下次 wrap 把 §1 Cowork 沙盒規格搬去 journal。

## 2026-09-11（第三筆）— 四頁加回 VOICEVOX 署名
- Why:討論「網站公開好不好」時指出唯一明確違反條款的是 08-23 移除的署名,使用者決定加回。
- 做法:minna-notes／kana 插在 </body> 前的小字段落;n5-vocab／n5-mock 併進既有 footer。角色從各頁 vv-data 的 speaker id 自動判定
  (minna/kana/mock = 2/11/13,n5-vocab = 14),格式「VOICEVOX:角色名」。三支測試全過。
- 同場討論未做:帳號密碼(靜態站做不到真正的鎖,不建議)、noindex、GoatCounter 瀏覽統計(要使用者先註冊)。

## 2026-09-11（第四筆）— 第6課單字進站、各課動詞表、表格編輯規則、favicon 與分頁熊貓
- 第6課:課名「いっしょに 行きませんか」＋單字 71 字(課本 48＋会話 2＋手寫補充 21)。課本字填標準重音、補充填「待確認」(同第5課);
  引擎試唸全部漢字詞只有「何ですか。」錯(ナニ→ナン)→ 加進 sayForceKana。補音檔 435 clips(say 2028→2173),檔案 28.9MB。
  課本重複前幾課的字(食べます、水…)照課本保留,總表自動去重(同 英語/靴 前例);手寫頁一行「何様にあいます」看不清沒收。
- 各課「動詞」分頁加本課動詞表(`lessonVerbTable()`,挑法同小抽考:假名以ます結尾),欄位同單字表、點格子發音、表頭勾選遮欄(`jp_lv_colmask`);
  唯讀模式下備註欄也是訪客備註,跟單字表共用 `lesson|word|kana` 的 sig,打字即時互相同步(單字表不會重畫,所以在 input 監聽器裡直接改另一張表)。
- 使用者要求的表格規則:拿掉「用法與補充」副標;備註欄兩種模式都黃底(#fffdf5);單字表只有備註可雙擊編輯(word/kana/mean 改成 get 唯讀)
  → 副作用:本機「＋ 新增單字」按鈕加出的列打不了字(新增一律走腳本)。動詞小抽考「看中文→猜日文」答案不再附中文(各課與總表都改)。
- favicon:使用者給 LINE 貼圖(NishimuraYuji GOKIGEN PANDA)截圖,用投影切成 17 隻、外圍白底 flood fill 透明;`docs/favicon.png` 用 1 號,六頁各加
  `<link rel="icon">`。課程分頁標籤左邊放熊貓(第1〜12課=2〜13 號、數字 14、總學習 15,`docs/img/tab/`,56px 共 66KB,16 號未用)。
  已告知授權疑慮(貼圖授權限 LINE 內使用),使用者決定沿用;替代方案(自畫 SVG／emoji)待他要時再換。
- 踩坑:動詞表模板把 masked class 串進 data-say 屬性(字串拼接順序);Playwright `.lvtable tr` 第一列是 thead。

## 2026-09-11（第五筆）— 動詞小抽考「加入單字」、第6課文法／朗讀／測驗補齊，檔案 40MB
- 「加入單字」：各課動詞小抽考多一個勾選，出題池加上「名詞＋助詞＋動詞×4 時態」。搭配表放 `<script id="vobj-data">`（key `word|kana`
  → [助詞, 名詞 word, 中文]），31 個動詞 139 組，人工挑選（隨機配會出「机を 食べます」）；名詞只取到本課為止，所以第5課的 食べます
  配不到第6課的 ごはん。音檔文本＝名詞假名＋助詞＋空格＋活用形，export_missing_clips.py 加 E 段同規則產生。
  **代價：1,650 clips → +8.4MB**（整句比單字長很多，估算時少算了），使用者選「照現狀」，音檔外置排下一件。
- 第6課文法 7 點（依手寫筆記：を受詞、〜をします、何をしますか、なん／なに、疑問詞＋も＋否定、＋でも＋肯定、肯否複習），
  QUIZ_GRAMMAR[6] 15 題、gen_grammar_quiz T[6] 43 條（受詞按動詞分組 BUY/MEDIA/FOOD/DRINK/READ/WRITE/LISTEN、SCOPE[6] 跨全課）→ 107 題；
  gen_reading rtmpl6 16 條（一套 22 行剛好一輪，多放的樣板永遠輪不到）→ 3 套；朗讀總表重產。課本的 で／ませんか／ましょう 等老師教到再補。
- 引擎唸法：「何でも」→ナニデモ，朗讀句直接寫假名なんでも（朗讀文本＝合成文本，沒有 say 欄可以改）。
- test_voice_full.mjs 原本寫死第6課是空課 → 改第7課；文法小考檢查涵蓋 1〜6 課。
- 三批音檔共 2,103 clips，minna-notes 28.9 → 40.3MB。

## 2026-09-11（第六筆）— 音檔外置：三頁 clip 拆成 docs/audio/<頁>/<id>.ogg
- Why：minna-notes 內嵌音檔漲到 40.3MB（「加入單字」一次 +8.4MB），照這速度第12課會破 GitHub 單檔 100MB；使用者拍板做外置。
- 做法：每個 clip 一個獨立 .ogg（不打包 JSON —— `file://` 下 fetch 讀不到旁邊檔案，但 `<audio src>` 可以；也省掉 base64 33%）。
  `vv-data` 只留 say＋reading；前端 `VV_BASE='audio/minna/'`、`VV_AUDIO` 改成由 say 建出的 id 集合（既有的 `if(id&&VV_AUDIO[id])` 判斷不用動），
  `VV_PLAYER.src=VV_BASE+id+'.ogg'`。kana／n5-vocab 同法（n5-vocab 的 vvClip 直接回傳 URL）。
- 管線：export 看資料夾判缺、merge 寫 .ogg＋只更新 say，資料夾名兩支都從 HTML 的 VV_BASE 讀；本機補音檔.py 用法不變。
  測試各加一項「say 指到的檔案都在」（node fs）；一開始把它加在總結迴圈之後沒被算到，搬到前面才生效。
- 結果：HTML 40.3→0.8MB／2.0→0.09MB／7.0→0.19MB；`docs/audio/` 11,526 檔 36MB。minna 38、kana 20、autoplay 全 PASS；
  假 clip 在複本上驗過 merge。n5-mock 的 `var VV`（2.2MB）未改。
- 順手：toolchain 刪掉 Cowork 沙盒規格（Ubuntu 22.04／Python 3.10／Node 22／磁碟 9.1GB／RAM 3.8GB／uid 1012 非 root／
  網路隔離連不到 localhost）與環境表兩列，回到 199 行；todo 的音檔外置移進 Archive。

## 2026-09-11（第七筆）— 助詞分頁改版：三張表全課共用、依課次上色
- Why：原本只有第5課一張「名詞＋助詞＋動詞」表（9 助詞 15 列）。使用者要：(1) 另外兩類（名詞＋助詞＋名詞／句尾與其他）同格式；
  (2) 每一課都有助詞分頁，本課新學上底色、之前學過正常、還沒學反灰，到第25課就全亮。
- 做法：`JOSHI_ALL` 一份模板，每列 `data-les`＝課本教到的課次；`loadJoshi()` 依目前課次加 `jo-key`／`jo-future`，筆記欄前插「第N課」標籤；
  `subsFor` 改成第1〜12課都有助詞分頁。三表 28／6／5 列、75 例句（新句子補 123 clips）。
- 課次判斷（使用者可改）：バスに 乗ります 標 5（手寫筆記教的，課本 16）、は 對比 標 6、よ 標 6（いいですよ）、で 原因 標 39、を 離開／穿越 標 16、と 引用 標 21。
- 舊表的「6」「9」「之後」小標籤與課次標籤重複，拿掉。
- 追加：總學習多「助詞總表」分頁（`loadAllJoshi()`，同一份 JOSHI_ALL，不上色、保留課次標籤）。

## 2026-09-12 — 模擬考選項朗讀退回瀏覽器語音的修正
- 症狀：問題3・4 的選項朗讀不是 VOICEVOX。原因：選項每次洗牌，但音檔鍵是「N|1ばん。選項」整句，洗牌後號碼接到別的選項就查不到 → TTS。
  之前的覆蓋測試用未洗牌的 BANK，所以沒抓到。
- 修法：`audioLines()` 把號碼「1ばん。」與選項文字拆成兩段 clip；`mock_audio.py` 同步並加 `--prune`（刪 86 個舊整句 clip、新合成 77 個，2.26→2.01MB）；
  `test_mock.mjs` 加「抽出的考卷洗牌後也全覆蓋」（36 項）。另用 spy 隨機抽三份考卷模擬播完：TTS 0 次。
- 追加（09-12）：大題標題加「大題 N・第 a〜b 題」小標（使用者把「問題2」當成第 2 題）；文法・読解 20 篇文章加 `passZh` 全文翻譯，
  交卷後顯示在文章下方（問題3 保留【1】【2】並附正解）。坑：`shuffleChoices()` 是白名單複製欄位，新欄位要記得加進去。

## 2026-09-13 — 第6課剩下的文法（課本 p.84 練習A 3〜5）
- 依課本照片＋手寫註記加 ⑧ 場所で 動作／⑨ 〜ませんか（邀請，含答應・婉拒回答）／⑩ 〜ましょう（提議）：`GRAMMAR_DEFAULT[6]` 7→10 點、
  `QUIZ_GRAMMAR[6]` 15→23 題、`gen_grammar_quiz.py` T[6] 43→59 條（107→178 題，總 533）、`gen_reading.py` rtmpl6 改寫（で／ませんか／ましょう 排前面，
  邀請句與「ええ、〜ましょう」回覆共用同一個動詞）、第6課朗讀 3 套與總表重產、音檔 +321 clips（9,066）。
- 助詞表的「で 發生舞台」原本就標第6課，不用動。測試 voice 38／kana 20／autoplay 全過。
- 坑：rtmpl6 是「依序取到 22 行」，新樣板放尾巴永遠輪不到 → 同類句用 c([...]) 合併成一格。

## 2026-09-14 — 第7課課名＋單字進站；第5・6課文法小考一輪抽一半
- 第5・6課文法小考一輪只抽一半（每個文法點各抽 ceil(n/2)，`GQ_HALF`／`gqDraw`），題庫不動；重考換一組。commit bf649c6。
- 第7課「いらっしゃい」：単語 38＋会話 6＋練習C 1＝45 列（重音照課本；片假名補來源），課名 `names['7']`；第6課手寫補充的「教えます」重音 待確認→3。音檔 +249 clips（9,315）。
- 坑：帶「〜」的片語（［〜は］いかがですか。）HTML 的 sayText 會去掉 U+301C 波浪號，但 export_missing_clips 的 STRIP 沒有 → 文本對不上、測試 FAIL。
  兩處都修：exporter 補上「〜」；那兩句改成可直接唸的「いかがですか。」「すてきですね。」（課本寫法放備註）。

## 2026-09-14 — 第6課補 5 字；文法小考 ① 改吃「加入單字」配對
- 使用者反映第6課文法小考重複率高、都是文法例句的字（沒看到 掃除／教えます／手紙／出します／撮ります）。
  改法：`gen_grammar_quiz.py` 第6課 ① 受詞題不再用固定樣板，改 `vobj_items()` 讀 `vobj-data`（該課 pos=verb 的動詞 × 它能搭的名詞，
  名詞要在該課或之前教過），ます／ました／ません 輪流 → 98 句；前端 `gqDraw` 每個文法點一輪最多 12 題（第6課一輪 74、第5課 78）。
- 第6課補 分かります／ゲーム／アニメ／漫画／医学，vobj 加 5 組配對（ゲームを します 等）。音檔 +336 clips（9,651）。
